spring:
  application:
    name: spring-transactional
  datasource:
    url: jdbc:mysql://localhost:3306/statistics?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
server:
  port: 8090
  logger:
    enabled: true
logging:
  config: classpath:log4j2-dev.yml
api:
  host_url: https://gitlab.com
  branch_name: dev
